/*package xyz.gnarbot.gnar.utils;

import net.dv8tion.jda.api.entities.Guild;
import xyz.gnarbot.gnar.db.guilds.GuildData;

public class LogHelper {

    public static void tryLogging(String message, GuildData options, Guild guild, String eventType) {
        if(options.getLog().getChannels().get(eventType) != null) {
            guild.getTextChannelById(options.getLog().getChannels().get(eventType)).sendMessage(message).queue();
        }

    }

}*/
